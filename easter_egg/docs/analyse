struct	command
{
  char	*cmd;
  char	**keyWords;
};

struct	listCmd
{
  int	nb;
  char	*cmd;
  char	*request;
};

struct		Plate
{
  int		numPlate;
  struct Plate	*next;
  struct Plate	*prev;
};

struct		Towers
{
  struct Plate	*plate;
};